// Root project. The loader independent modules and every published artifact are
// subprojects; nothing is built here on purpose, so that a failing Minecraft
// generation can never hide a core test failure.
